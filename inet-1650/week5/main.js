// alert create a dialog

alert( 'Hello, world!' );

/* 
alerts should be avoided because they can be rather annoying for the user; they require the user to actually click them before doing anything else.  Some browsers even block them if you try to create too many too quickly.
*/
